import { ImagenEquipoPipe } from './imagen-equipo.pipe';

describe('ImagenEquipoPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenEquipoPipe();
    expect(pipe).toBeTruthy();
  });
});
